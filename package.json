{
  "name": "Phone-Ledger",
  "private": true,
  "description": "Phone Ledger - Manage your mobile business in one place",
  "author": "Zaid Sunasra",
  "scripts": {
    "dev": "pnpm --parallel --filter ./client --filter ./server dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "oxlint": "^1.83.0"
  },
  "packageManager": "pnpm@11.21.0"
}
